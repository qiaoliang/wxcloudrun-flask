# run all local tests for backend

执行下面的shell 命令。

```bash
cd ~/working/code/safeGuard/backend
source venv_py312/bin/activate
pip install -r requirements.txt
pip install -r requirements-test.txt
python scripts/run_tests.py
```
